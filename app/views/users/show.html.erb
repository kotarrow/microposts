<h1>
  <%= gravatar_for @user %>
  <%= @user.name %>
  <%#= binding.pry %>
  <%# @userはid,name,email,password_digest,created_at,updated_at %>
</h1>

<div class="row">
  <div class="col-md-6 col-md-offset-3">
    <%= form_for(@user) do |f| %>
    <%= f.submit "profile edit", class: "btn btn-large btn-primary" %>
    <% end %>
  </div>
</div>